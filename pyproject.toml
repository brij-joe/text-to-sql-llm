[project]
name = "prompt-tuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.13.0",
    "datasets>=4.8.4",
    "peft>=0.18.1",
    "python-dotenv>=1.2.2",
    "transformers>=5.4.0",
]
